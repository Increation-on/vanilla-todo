/**
 * МОДУЛЬ: DOM МАНИПУЛЯЦИИ (dom-manipulation.js)
 * 
 * ЗАЧЕМ НУЖЕН: Безопасные операции с DOM, защита от ошибок
 * 
 * АРХИТЕКТУРА: Утилиты-обертки над нативными DOM-методами
 * - Защита от неправильных аргументов
 * - Единая точка для будущих улучшений (анимации, логирование)
 * - Чистые функции (предсказуемый результат)
 */

/**
 * Безопасно удаляет элемент из DOM
 * 
 * ЗАЩИЩАЕТ от: 
 * - element = null/undefined
 * - element без метода .remove() (не DOM-элемент)
 * 
 * ПРЕИМУЩЕСТВА:
 * - Не падает с ошибкой при неправильных данных
 * - Можно добавить анимацию удаления в одном месте
 * - Семантичное имя (яснее чем element.remove())
 * 
 * ИСПОЛЬЗОВАТЬ ВМЕСТО: прямого вызова element.remove()
 */
export const deleteElement = (element) => {
    // Защита от передачи мусора вместо DOM-элемента
    if (!element || !element.remove) {
        console.warn('deleteElement: invalid element', element)
        return // Тихий выход вместо падения приложения
    }
    element.remove()
}

/**
 * Безопасно переключает CSS-класс у элемента
 * 
 * ЗАЩИЩАЕТ от:
 * - element = null/undefined  
 * - className не строка
 * - Отсутствия classList у элемента
 * 
 * ПРЕИМУЩЕСТВА:
 * - Единое место для работы с классами
 * - Можно добавить логирование изменений
 * - Подготовка к возможному переходу на CSS-in-JS
 * 
 * ИСПОЛЬЗОВАТЬ ДЛЯ: добавления/удаления любых CSS-классов
 */
export const toggleClass = (element, className) => {
    // Двойная защита: проверяем и элемент и тип класса
    if (!element || typeof className !== 'string') return
    element.classList.toggle(className)
}

// 💡 КОММЕНТАРИЙ ДЛЯ РАЗРАБОТЧИКА:
// Эти функции - "чистые" утилиты. Они:
// - Не изменяют глобальное состояние
// - Всегда возвращают одинаковый результат для одинаковых входных данных
// - Легко тестируются изолированно
// - Не знают о бизнес-логике приложения