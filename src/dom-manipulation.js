/**
 * МОДУЛЬ: DOM МАНИПУЛЯЦИИ
 * Ответственность: низкоуровневые операции с DOM
 * Паттерн: Utility/Helper Functions
 */

// Функция для удаления элемента из DOM
export const deleteElement = (deletedElement) => {
    deletedElement.remove()
    // 🎯 ПАТТЕРН: Абстракция над нативными методами
    // Прячем прямой вызов .remove() за семантичным именем
    // 💡 В будущем можно добавить анимацию удаления не меняя API
}

// Функция для переключения CSS-класса у элемента  
export const toggleClass = (element, className) => {
    element.classList.toggle(className)
    // 🎯 ПАТТЕРН: Единая точка для работы с CSS классами
    // Упрощает миграцию на CSS-in-JS если понадобится
    // 🔧 УНИВЕРСАЛЬНОСТЬ: Работает с любым элементом и классом
}

// 💡 ЗАМЕТКА: Эти функции - "чистые" утилиты
// Не зависят от состояния приложения, не имеют side effects
// Можно тестировать изолированно

// 🚨 ДЫРА: Нет обработки edge cases
// - Что если element = null?
// - Что если className не строка?
// В продакшене добавили бы валидацию:
// if (!element || typeof className !== 'string') return