// toast.js

// 🚀 ЭКСПОРТ функции для показа всплывающих уведомлений
export const showToast = (message, type = 'info') => {
    // 🎪 СОЗДАЕМ ЭЛЕМЕНТ УВЕДОМЛЕНИЯ
    const toast = document.createElement('div')           // 📝 Создаем div для уведомления
    toast.textContent = message                           // 📢 Заполняем текстом из параметра
    toast.className = `toast toast-${type}`               // 🏷️ Добавляем CSS-классы для стилизации
    
    // 🎨 НАСТРАИВАЕМ СТИЛИ ЧЕРЕЗ JavaScript
    Object.assign(toast.style, {                          // 📦 Копируем все стили сразу
        position: 'fixed',                                // 📍 Фиксируем относительно окна браузера
        top: '20px',                                      // 🔝 Отступ сверху 20px
        right: '20px',                                    // 🔜 Отступ справа 20px
        padding: '12px 20px',                             // 📐 Внутренние отступы
        borderRadius: '4px',                              // 🔵 Закругленные углы
        color: 'white',                                   // ⚪ Белый текст
        zIndex: '1000',                                   // 🔼 Поверх всех элементов
        transition: 'opacity 0.3s'                        // 🎭 Плавное изменение прозрачности за 0.3s
    })
    
    // 🌈 НАСТРАИВАЕМ ЦВЕТ В ЗАВИСИМОСТИ ОТ ТИПА УВЕДОМЛЕНИЯ
    const colors = {                                      // 🎨 Палитра цветов для разных типов
        error: '#f44336',                                 // 🔴 Красный для ошибок
        success: '#4caf50',                               // 🟢 Зеленый для успеха
        info: '#2196f3'                                   // 🔵 Синий для информации
    }
    toast.style.backgroundColor = colors[type] || colors.info  // 🎯 Берем цвет по типу или синий по умолчанию
    
    // 📍 ДОБАВЛЯЕМ УВЕДОМЛЕНИЕ НА СТРАНИЦУ
    document.body.appendChild(toast)                      // ➕ Вставляем toast в конец body
    
    // ⏰ НАСТРАИВАЕМ АВТОМАТИЧЕСКОЕ УДАЛЕНИЕ
    setTimeout(() => {                                    // ⏱️ Через 2000ms (2 секунды):
        toast.style.opacity = '0'                         // 🎭 Начинаем плавное исчезновение
        setTimeout(() => {                                // ⏱️ Еще через 300ms (когда анимация завершится):
            if (toast.parentNode) {                       // 🔍 Проверяем, что toast еще на странице
                toast.parentNode.removeChild(toast)       // 🗑️ Удаляем элемент из DOM
            }
        }, 300)
    }, 2000)
}