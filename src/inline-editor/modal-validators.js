// ✅ Функция валидации ввода для inline-редактора
// 📌 Проверяет, можно ли сохранить измененный текст задачи
export function validateInput(text, currentText) {
    // 🧹 Очищаем текст от лишних пробелов в начале и конце
    const newText = text.trim()
    
    // 🔍 Проверяем условия валидности:
    //   - newText не должен быть пустой строкой (после trim)
    //   - newText должен отличаться от исходного текста
    const isValid = newText && newText !== currentText
    
    // 📤 Возвращаем объект с результатами валидации:
    return {
        isValid: isValid,          // ✅ true если текст можно сохранить
        cleanedText: newText       // 🧹 Очищенная версия текста (без пробелов)
    }
}