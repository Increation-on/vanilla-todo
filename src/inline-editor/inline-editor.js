import { openEditModal } from './modal-creator.js'
import './inline-editor.css'

export function initInlineEditor(editButton, taskId, currentText, onTextUpdate) {
    // 🎯 Основная функция инициализации inline-редактора
    // 📌 Принимает: 
    //   - editButton: кнопка, которая открывает редактор
    //   - taskId: идентификатор задачи для редактирования  
    //   - currentText: текущий текст задачи
    //   - onTextUpdate: колбэк, который вызовется после сохранения изменений
    
    editButton.addEventListener('click', (e) => {
        // 🛡️ Предотвращаем всплытие события, чтобы не мешать другим обработчикам
        e.stopPropagation()
        
        // 🚀 Открываем модальное окно редактирования
        // 📌 Вся сложная логика вынесена в отдельный модуль modal-creator
        openEditModal(taskId, currentText, onTextUpdate)
    })
}