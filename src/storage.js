/**
 * МОДУЛЬ: ХРАНИЛИЩЕ ДАННЫХ (storage.js)
 * 
 * ЗАЧЕМ НУЖЕН: Единый источник истины для данных приложения
 * 
 * АРХИТЕКТУРА: 
 * - Состояние в памяти (быстрый доступ)
 * - Синхронизация с localStorage (сохранение между сессиями)
 * - Иммутабельные обновления (предсказуемость)
 * 
 * ПРИНЦИП: Single Source of Truth - все данные проходят через этот модуль
 */

// 🎯 СОСТОЯНИЕ ПРИЛОЖЕНИЯ В ПАМЯТИ
// Глобальная переменная модуля - единственное место где хранятся задачи
let tasks = [];

/**
 * ЗАГРУЗКА ДАННЫХ ИЗ LOCALSTORAGE ПРИ ЗАПУСКЕ
 * 
 * БЕЗОПАСНАЯ ЗАГРУЗКА:
 * - Проверяет наличие данных
 * - Обрабатывает невалидный JSON
 * - При ошибке начинает с чистого листа
 * 
 * ВЫЗЫВАЕТСЯ: Автоматически при импорте модуля
 */
const loadFromStorage = () => {
    try {
        const saved = localStorage.getItem('tasks');
        if (saved) {
            tasks = JSON.parse(saved);
        }
    } catch (error) {
        console.error('Ошибка загрузки из localStorage:', error);
        tasks = []; // 🔁 Начинаем заново при поврежденных данных
    }
};

// 🚀 НЕМЕДЛЕННАЯ ЗАГРУЗКА ПРИ ЗАПУСКЕ ПРИЛОЖЕНИЯ
loadFromStorage();

/**
 * СОХРАНЕНИЕ ДАННЫХ В LOCALSTORAGE
 * 
 * БЕЗОПАСНОЕ СОХРАНЕНИЕ:
 * - Обрабатывает ошибки (переполнение, приватный режим)
 * - Не падает при проблемах с localStorage
 * 
 * ВЫЗЫВАЕТСЯ: После КАЖДОГО изменения tasks
 */
const saveToStorage = () => {
    try {
        localStorage.setItem('tasks', JSON.stringify(tasks));
    } catch (error) {
        console.error('Ошибка сохранения в localStorage:', error);
    }
};

/**
 * ПОЛУЧЕНИЕ ВСЕХ ЗАДАЧ (READ-ONLY)
 * 
 * ВОЗВРАЩАЕТ: Копию массива задач
 * ЗАЧЕМ КОПИЯ: Защита от случайных изменений исходных данных
 * 
 * ИСПОЛЬЗОВАТЬ: Когда нужно прочитать задачи без изменения
 */
export const getTasksFromStorage = () => {
    return [...tasks]; // 🛡️ Защитная копия
};

/**
 * ДОБАВЛЕНИЕ НОВОЙ ЗАДАЧИ
 * 
 * ПОДДЕРЖИВАЕТ ДВА ФОРМАТА:
 * - String: "Текст задачи" → создает полный объект
 * - Object: {text: "Текст"} → дополняет недостающие поля
 * 
 * АВТОМАТИЧЕСКИ ДОБАВЛЯЕТ:
 * - Уникальный ID (временная метка)
 * - Время создания
 * - Источник (user/api) для аналитики
 */
export const addTaskToStorage = (taskData) => {
    const newTask = typeof taskData === 'string' 
        ? {
            // 🎯 Задача от пользователя (из интерфейса)
            id: Date.now().toString(),
            text: taskData.trim(),
            completed: false,
            createdAt: new Date().toISOString(),
            source: 'user'
        }
        : {
            // 🎯 Задача из API (уже имеет структуру)
            ...taskData,
            id: taskData.id || Date.now().toString(),
            createdAt: taskData.createdAt || new Date().toISOString(),
            source: taskData.source || 'api'
        };
    
    // 🎯 Иммутабельное обновление (создаем новый массив)
    tasks = [...tasks, newTask];
    saveToStorage();
    return newTask; // 🔄 Возвращаем созданную задачу для дальнейшего использования
};

/**
 * УДАЛЕНИЕ ЗАДАЧИ ПО ID
 * 
 * КАК РАБОТАЕТ: Фильтрует массив, оставляя все КРОМЕ удаляемой
 * АВТОСОХРАНЕНИЕ: Сразу после изменения
 * 
 * ИСПОЛЬЗОВАТЬ: При клике на кнопку удаления
 */
export const removeTaskFromStorage = (id) => {
    tasks = tasks.filter(task => task.id !== id);
    saveToStorage();
};

/**
 * ПЕРЕКЛЮЧЕНИЕ СТАТУСА ВЫПОЛНЕНИЯ ЗАДАЧИ
 * 
 * КАК РАБОТАЕТ: 
 * - Находит задачу по ID
 * - Создает новую задачу с инвертированным completed
 * - Сохраняет все остальные свойства без изменений
 * 
 * ИММУТАБЕЛЬНОСТЬ: Не изменяет исходную задачу, создает новую
 */
export const toggleTaskStatus = (id) => {
    tasks = tasks.map(task => {
        if (task.id === id) {
            return {
                ...task, // 🎯 Копируем все свойства
                completed: !task.completed // 🔄 Инвертируем статус
            };
        }
        return task;
    });
    saveToStorage();
};

export const updateTaskText = (id, newText) => {
    const trimmedText = newText.trim()
    if (!trimmedText) return // 🛡️ защита от пустого текста
    
    tasks = tasks.map(task => 
        task.id === id 
            ? { ...task, text: trimmedText }
            : task
    )
    saveToStorage()
}

// 💡 АРХИТЕКТУРНЫЙ КОММЕНТАРИЙ:
// Этот модуль - единственное место в приложении, где можно изменить tasks.
// Все изменения проходят через экспортированные функции, что делает
// состояние предсказуемым и отлаживаемым.

// 🔮 ВОЗМОЖНЫЕ УЛУЧШЕНИЯ:
// - Миграция данных при изменении формата
// - Конфликт-резолюция при синхронизации с сервером
// - История изменений (undo/redo)